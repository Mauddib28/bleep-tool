"""bleep.protocols – Low-level protocol implementations.

This package contains protocol-layer code that operates *below* the D-Bus
layer — directly on raw sockets (RFCOMM, L2CAP) rather than through BlueZ
obexd or other system daemons.

Currently contains design documentation only.  Implementation is planned
for a future version.
"""
